"""周期股分析 skill（占位 — P3 阶段实现完整逻辑）。"""
from pathlib import Path

SKILL_DIR = Path(__file__).parent

DESCRIPTION = "周期股分析：周期定位 + 估值 + 风险（占位）"


def run(state, llm) -> dict:
    """占位实现：返回空 state delta。"""
    return {}